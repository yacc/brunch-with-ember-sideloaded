/*jslint browser: true, jquery: true */ 
/*global App: false, require: false, Ember: false*/

App = require("app");
App.{{#camelize}}{{name}}{{/camelize}}Route = Ember.Route.extend({
  
	model: function() {
    	return App.{{#camelize}}{{name}}{{/camelize}}.find();
  	}
  
});
